[pytest]
# Keep discovery inside the test suite. Otherwise pytest walks container-owned
# MySQL runtime files under data/ and fails before collecting application tests.
testpaths = tests
pythonpath = . crawler/src
addopts = -ra
